# 蒙自城镇供水 (Mengzi Water)

Home Assistant 自定义集成,用于查询**蒙自市城镇供水有限责任公司**网上营业厅的户号数据:预存金额、待缴欠费、缴费时间、供水状态等。

> ⚠️ **非官方集成**。只读查询,不做缴费/转账。数据通过网上营业厅 H5 接口获取,接口可能随时调整;会话 Cookie 仅保存在本机 HA 配置中,不会上传到任何第三方。

## 功能特性

- 读取全部**已绑定户号**(微信营业厅内绑定的户号,一个集成同时管理)
- 每个户号提供传感器:**预存金额 / 待缴欠费合计 / 待缴账单数 / 最近缴费时间 / 供水状态**
- **账单统计(按账期自动聚合)**:本月、本季、本年用水量(m³)与水费(元),以及最近一期账单用量/金额;费用分解(水费/污水处理费/垃圾处理费/滞纳金)、表码与近 12 期账单明细见属性
- **水价标准**:自动拉取营业厅《水价公示》文章,标题为状态、公示内容(阶梯价目表)见属性
- 户号/户名/用址/水表数/用水类型/册名等以**属性**形式附加在每个传感器上,界面简洁
- 轮询间隔可配置(默认 30 分钟,最低 5 分钟)
- 会话失效自动标记“需要重新认证”,无需删除重配

> 说明:统计口径为自然月/季/年(以账单“账期”为准,如 2026-09);收费明细按账单返回字段(实测含自来水费、污水处理费、垃圾处理费、滞纳金)。

## 安装

### HACS(推荐)

1. HACS → 右上角 `⋯` → **自定义存储库 (Custom repositories)**
2. 存储库地址填 `https://github.com/ly2199/ha_mengzi_water`,类别选 **集成 (Integration)** → 添加
3. 搜索 **蒙自城镇供水 (Mengzi Water)** → 下载 → 重启 Home Assistant

### 手动安装

将 `custom_components/mengzi_water/` 整个文件夹复制到 HA 配置目录的 `custom_components/mengzi_water/` 下,重启。

## 配置(获取会话 Cookie)

本系统登录依赖微信网页授权,HA 无法自主登录,需要一次性抓取会话 Cookie:

1. 手机微信打开 `https://swp.mzczgs.com/Hall/`,完成授权登录并确认能看到户号数据
2. 手机设置代理指向电脑上的抓包工具(**mitmproxy / Charles** 等),回到微信重新打开一次营业厅首页
3. 在抓包工具中找到发往 `swp.mzczgs.com` 的任意 POST 请求,复制请求头 **Cookie** 中 `Ares.Core.Session.JXSWPHALL=...` 的值(整段 `名=值` 粘贴即可,容忍整段 Cookie)
4. HA:设置 → 设备与服务 → 添加集成 → **蒙自城镇供水** → 粘贴 Cookie → 提交(向导会实时校验并回显户号信息)

之后集成按设定间隔自动轮询;数据仅保存在本机 HA 的配置条目中(与其他凭据同等加密存放)。

## 会话失效怎么办

- 集成会显示“需要重新认证”错误,进入 设备与服务 → 该集成 → 重新认证
- 重新按上述步骤抓一次最新 Cookie 粘贴即可;老 Cookie 有效期取决于供水公司服务端(实测随会话而定),无需频繁更新

## 传感器

每个绑定户号一个设备(设备名含户号),设备下包含:

| 实体(翻译名) | 说明 |
|---|---|
| 预存金额 | 账户预存余额(元) |
| 待缴欠费合计 | 待缴账单合计金额(元) |
| 待缴账单数 | 未缴费账单数量(笔) |
| 最近缴费时间 | 最近一次缴费时间 |
| 供水状态 | 正常 / 停水 |
| 本月/本季/本年用水量 | 按账期聚合的用水量(m³) |
| 本月/本季/本年水费 | 按账期聚合的总费用(元) |
| 最近一期用水量 / 水费 | 最近一张账单(期/费用分解/表码见属性) |

集成级实体:

| 实体 | 说明 |
|---|---|
| 水价标准 | 营业厅《水价公示》文章标题,公示内容(含阶梯价目表)在属性中 |

户号/户名/用址/册名/用水类型/水表数/更新时间等作为每个传感器的**附加属性**提供;统计类传感器的属性里另含最近账单期、费用分解、表码与近 12 期账单明细。

> 实体 ID 形如 `sensor.mengzi_water_<户号>_usage_year`,多户号互不干扰。

## 数据来源与免责声明

- 数据来源:蒙自市城镇供水网上营业厅 H5(`https://swp.mzczgs.com/Hall/`,与微信公众号同源)
- 本项目**非蒙自市城镇供水官方**发布,仅供个人学习与家庭自动化使用
- 请勿设置过短的轮询间隔,以免对服务端造成压力
- 平台接口如有调整导致失效,请在 [Issues](https://github.com/ly2199/ha_mengzi_water/issues) 反馈

## 许可

保留所有权利 (All rights reserved)。未经作者许可,请勿用于商业用途。
